# 验证范围

运行 `python demo/run_demo.py` 会验证以下行为：

- 工作流参数替换保留原模板；无效节点绑定被拒绝。
- 模拟服务接收提交，经历等待后返回结果元数据。
- 模拟任务失败被报告，持续等待在时限内结束。
- 字幕起止时间按给定值偏移，正文保持一致；倒置时间被拒绝。
- 三语正例通过；缺拼音音节或缺英文行的负例被识别。

实际结果见 `demo/output/report.json`。三份 Skill 另做元数据、命名、资源链接和脚本入口检查。

2026-09-04 本机运行结果：11/11 检查通过；3/3 Skill 格式校验通过。三个脚本的命令行入口分别完成计划生成、SRT 偏移和三语正例检查。

全部输入为通用合成样例。测试仅访问临时本机回环服务，不调用公网服务、生成模型或账号接口。真实 ComfyUI、GPU 环境和视频生成质量不属于此次验证范围。
